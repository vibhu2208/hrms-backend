# HRMS Backend Environment Configuration
# Copy this file to .env and fill in your actual values

# Database Configuration
MONGODB_URI=mongodb://localhost:27017/hrms
REDIS_URL=redis://localhost:6379

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-here
JWT_EXPIRE=7d

# Email Configuration
EMAIL_SERVICE=gmail
EMAIL_USERNAME=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
EMAIL_FROM=your-email@gmail.com

# AWS S3 Configuration (for resume storage)
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
AWS_REGION=ap-south-1
AWS_S3_BUCKET=your-s3-bucket-name

# Reducto AI Configuration (Production Ready)
REDUCTO_API_KEY=your-reducto-api-key-here
REDUCTO_BASE_URL=https://platform.reducto.ai

# Reducto Performance & Reliability Settings
REDUCTO_MAX_RETRIES=3
REDUCTO_TIMEOUT=120000
REDUCTO_RETRY_DELAY=2000
REDUCTO_MAX_FILE_SIZE=10485760
REDUCTO_MAX_CONCURRENT=5
REDUCTO_BATCH_SIZE=10

# Reducto Caching (Performance Optimization)
REDUCTO_ENABLE_CACHE=true
REDUCTO_CACHE_TTL=3600000

# Reducto Batch Processing
REDUCTO_ENABLE_BATCH=true

# Application Configuration
NODE_ENV=production
PORT=5000

# Multi-tenant Configuration
DEFAULT_TENANT_ID=default

# Logging
LOG_LEVEL=info

# Security
BCRYPT_ROUNDS=12
SESSION_TIMEOUT=3600000

# Rate Limiting
RATE_LIMIT_WINDOW=900000
RATE_LIMIT_MAX_REQUESTS=100

# CORS
CORS_ORIGIN=http://localhost:3000