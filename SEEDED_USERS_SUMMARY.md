# Seeded Users Summary

Credentials are **not** stored in this file.

Run `ALLOW_SEED=1 npm run seed:tcs` (or wipe-and-seed with `ALLOW_WIPE=1`) and capture
console output securely. See `TCS_CREDENTIALS.md` for expected emails and login paths.

If you previously used passwords from an older version of this document, **rotate them now**.
